# Airflow base is provided by the image; do not pin apache-airflow here.
apache-airflow-providers-apache-spark==5.3.2

# Optional
# if you plan to call MinIO/S3 from Python tasks
boto3==1.34.158   
# for simple HTTP calls in DAGs
requests==2.32.3  
