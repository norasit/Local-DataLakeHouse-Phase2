# --- Iceberg via Nessie ---
connector.name=iceberg
iceberg.catalog.type=nessie
iceberg.nessie-catalog.uri=http://nessie:19120/api/v2
iceberg.nessie-catalog.ref=main
# ค่า warehouse เริ่มต้นของ schema/table ที่ไม่ได้กำหนด location
iceberg.nessie-catalog.default-warehouse-dir=s3://lakehouse-data/warehouse
iceberg.file-format=PARQUET

# --- Native S3 for Trino (works with MinIO) ---
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.aws-access-key=test
s3.aws-secret-key=test12334567
s3.path-style-access=true
